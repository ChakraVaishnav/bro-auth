{
  "name": "bro-auth",
  "version": "0.1.3",
  "description": "bro-auth — Stateless, fingerprint-bound JWT authentication. Server utilities + browser fingerprinting module.",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "browser": "dist/browser.mjs",
  "exports": {
    "./core": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./browser": {
      "import": "./dist/browser.js",
      "require": "./dist/browser.cjs"
    }
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
  "clean": "rimraf dist",

  "build:server": "tsup src/index.js --format cjs,esm --out-dir dist --dts false --out-extension cjs=.cjs --out-extension esm=.js --target node",

  "build:browser": "tsup src/browser/index.js --format cjs,esm --out-dir dist --dts false --out-extension cjs=.cjs --out-extension esm=.js --platform browser --name browser",

  "build": "npm run clean && npm run build:server && npm run build:browser"
},
  "keywords": [
    "jwt",
    "authentication",
    "browser fingerprint",
    "token binding",
    "security",
    "bro-auth",
    "auth"
  ],
  "author": "Vaishnav",
  "license": "MIT",
  "dependencies": {
    "crypto-es": "^3.1.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "rimraf": "^5.0.10",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
